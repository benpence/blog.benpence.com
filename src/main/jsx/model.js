export class Post {
  constructor(id, title, createdDate, tags, content) {
    this.id = id
    this.title = title
    this.createdDate = createdDate
    this.tags = tags
    this.content = content
  }
}
